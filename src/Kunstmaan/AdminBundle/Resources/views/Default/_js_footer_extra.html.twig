<script src="{{ asset('bundles/webtownkunstmaanextension/lib/ui/jquery-ui.min.js') }}"></script>
<script src="{{ asset('bundles/ujhazpublic/js/jquery-ui-sortable.min.js') }}"></script>
<script src="{{ asset('bundles/webtownkunstmaanextension/js/adminsorter.js') }}"></script>
<script>
    $(function() {
        {% for data in kuma_disabled_node_fields %}
        $('[name$="[{{ data.inputname }}]"').each(function() {
            if ($(this).val() == '{{ data.value }}')
            {
                $(this).prop('readonly', true);
            }
        });
        {% endfor %}
    });
</script>
